// experimental.js | DLM: 4/11/2022 | No License.
// This JS file includes some basic component interactions, it´s for every sub-page 

console.log("JS Linked");

/* 
// This code needs to be revised.
    // Code to open and collapse a filter
    const filter_button = document.getElementById("filter_button");
    const filter = document.getElementById("filter");
    let toggle = false
    filter_button.addEventListener("click", ()=>{

        if (!toggle){
            addCssClass(filter,"open")
        } else {
            removeCssClass(filter,"open")
        }
        toggle = !toggle
    })

    // Code to open and collapse a dropdown
    const dropdown_button = document.getElementById("dropdown_button");
    const dropdown = document.getElementById("dropdown");
    toggle = false
    dropdown_button.addEventListener("click", ()=>{

        if (!toggle){
            addCssClass(dropdown,"open")
        } else {
            removeCssClass(dropdown,"open")
        }
        toggle = !toggle
    })


    // Functions to add and remove css classes.
    const addCssClass = (toTag,string) => {
        toTag.classList.add(string);
    }
    const removeCssClass = (toTag,string) => {
        toTag.classList.remove(string);
    }
*/